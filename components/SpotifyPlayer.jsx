import search from 'youtube-search';

export default function SpotifyPlayer({ ytPlayer, playerData, setPlayerData }) {

    return (
        <section className="song_player">
            {/* // */}
        </section>
    );
}